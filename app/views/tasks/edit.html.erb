<h1>Edit task</h1>

<!-- #Form_for has 3 main magic qualities
  # is decided on the path based on the object it gets. (if @restaurant == new object it will go to create)
  # it pre populates fields if need be
  # it creates a need little hash with your object already populated
  # does the tokens for you -->


<%= form_for @task do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title, class: "form-control form-group" %>

  <%= t.label :details %>
  <%= t.text_field :details, class: "form-control form-group" %>

  <%= t.label :completed %>
  <%= t.check_box :completed, class: "form-control form-group" %>

  <%= t.submit class: 'btn btn-primary' %>
<% end %>
